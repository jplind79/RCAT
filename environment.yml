name: rcatenv
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - xarray
  - xesmf>=0.8
  - esmpy
  - numpy
  - dask-jobqueue
  - dask[complete]
  - matplotlib
  - netcdf4
  - h5netcdf
  - cartopy
  - pip
  - pip:
    - rcatool
